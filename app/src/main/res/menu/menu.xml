<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:id="@+id/menu_configuracao"
        android:title="@string/app_configuracao"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:id="@+id/menu_sair"
        android:title="@string/app_sair"
        android:orderInCategory="200"
        app:showAsAction="never"/>

    <item android:id="@+id/menu_compartilhar"
        android:title="@string/app_compartilhar"
        android:icon="@drawable/ic_action_camera"
        android:orderInCategory="300"
        app:showAsAction="ifRoom"/>

</menu>